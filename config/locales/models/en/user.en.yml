en:
  activerecord:
    models:
      user: User
    attributes:
      user:
        full_name: Full name
    errors:
      models:
        user:
          attributes:
            password:
              too_short: Password is too short
              blank: Пароль не может быть пустым
            password_confirmation:
              confirmation: Enter your new password again
            email:
              blank: Email can't be blank
            full_name:
              blank: Full name can't be blank
            role:
              inclusion: Role must be user or admin
      messages:
        record_invalid: User is invalid
  enumerize:
    user:
      role:
        user: User
        admin: Admin
